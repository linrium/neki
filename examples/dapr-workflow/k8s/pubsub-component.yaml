apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: pubsub
  namespace: ${NAMESPACE}
spec:
  type: pubsub.redis
  version: v1
  metadata:
    - name: redisHost
      value: "${REDIS_HOST}"
    - name: redisPassword
      value: "${REDIS_PASSWORD}"
scopes:
  - dapr-workflow
