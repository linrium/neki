apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: hello-bun-ts-config
  namespace: ${NAMESPACE}
spec:
  tracing:
    samplingRate: "1"
    otel:
      endpointAddress: alloy.observability.svc.cluster.local:4317
      isSecure: false
      protocol: grpc
  metric:
    enabled: true

